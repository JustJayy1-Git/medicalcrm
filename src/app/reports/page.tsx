import { StubPage } from "../(stubs)/stub-page";

export default function ReportsPage() {
  return (
    <StubPage
      active="/reports"
      title="Reports"
      description="A/R aging, day sheets, productivity, payment summaries. Live, not batch."
    />
  );
}
