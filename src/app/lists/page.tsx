import { StubPage } from "../(stubs)/stub-page";

export default function ListsPage() {
  return (
    <StubPage
      title="Lists"
      description="Insurance carriers, attorneys, providers, CPT codes, ICD-10 codes — your master data lives here."
    />
  );
}
